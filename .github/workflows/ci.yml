name: Build

on:
  push:
    branches:
      - master
      - 2201.[0-9]+.x
  repository_dispatch:
    types: check_connector_for_breaking_changes

jobs:
  call_workflow:
    name: Run Connector Build Workflow
    if: ${{ github.repository_owner == 'ballerina-platform' }}
    uses: ballerina-platform/ballerina-library/.github/workflows/build-connector-template.yml@main
    secrets:
      BALLERINA_BOT_TOKEN: ${{ secrets.BALLERINA_BOT_TOKEN }}
      BALLERINA_BOT_USERNAME: ${{ secrets.BALLERINA_BOT_USERNAME }}
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    with:
      repo-name: module-ballerinax-aws.ses
      additional-build-flags: "-x :aws.ses-examples:build"
      additional-test-flags: "-x :aws.ses-examples:test"
